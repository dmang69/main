# Shennell AI Agent Platform

This repository contains:

- Backend API in `backend/` (FastAPI + MongoDB)
- Frontend app in `frontend/` (Expo React Native)

## Build and Release

For full multi-platform build analysis and setup steps (Android, iOS, and Windows strategy), see:

- `BUILD_SETUP_GUIDE.md`
